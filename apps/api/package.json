{
  "name": "@exile-toolkit/api",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "dev": "wrangler dev",
    "typecheck": "tsc -p tsconfig.json --pretty false"
  },
  "dependencies": {
    "@exile-toolkit/contracts": "workspace:*",
    "@exile-toolkit/data": "workspace:*",
    "@exile-toolkit/domain": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260823.1",
    "typescript": "^6.0.3",
    "wrangler": "^4.125.0"
  }
}
